eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
    service-url:
      default-Zone: http://eureka-server1/eureka/, http://eureka-server2/eureka/, http://eureka-server3/eureka/
  server:
    wait-time-in-ms-when-sync-empty: 5